node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# This is a Bun project — keep only bun.lock (no npm/yarn/pnpm lockfiles).
package-lock.json
yarn.lock
pnpm-lock.yaml

prisma/generated

docs/
